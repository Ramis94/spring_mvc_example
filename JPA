@OneToMany
...
...
...
    fetch = FetchType.Lazy - подгрузка через геттер
    fetch = FetchType.Eager - всегда

@Id - это поле является индентификатором

EntityManager - имеет методы по сохранению в БД, по скачиванию по получению списка
Session - завявывает на hibernate

проксирование

Criteria API

Кеш - запрос к БД очень дорогая операция, поэтому кешируем запросы, используется ассоциативный массив
3 уровня кэша:
    1) Уровень одной сессии, если мы во время сессии пытаемся получить один и тот же объект, то запрос к БД будет выполнен один раз, второй раз объект будет взят из кеша
    2) Кеш второго уровня нужно подключать в настройках, работает на уровне SessionFactory, запрос к бд будет выполнен один раз,
но эти два кеша не покрывают, когда у нас запросы есть на языке SQL, для того есть:
    3) Кеш запросов, нужно подключать отдельно

Для работы с JPA нужно 1)предоставить мета-информацию
2) поключить в градл зависимость
3) сконфигурировать EntityManager в файле persistance.xml
4) дальше можно использовать EntityFactory из EntityManager